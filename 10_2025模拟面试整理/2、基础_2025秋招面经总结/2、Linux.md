### 1. Linux常用命令：ls、cd、grep、ps、top、netstat的作用？ 

笔记：
- `ls`：列出该目录下所有子目录和文件
- `cd`：改变工作目录，到指定目录下
- `grep`：查找符合条件的字符串或正则表达式
- `ps`：显示当前进程状态
- `top`：提供系统动态实时视图
- `netstat`：显示网络状态，可显示**TCP/UDP连接、监听端口、路由表、接口统计**等信息

> [!NOTE] 记不住？
> 看全称，
> - **ls**——List
> - **cd**——Change Directory
> - **grep**——Global Regular Expression Print（全局 正则 表达式 打印）
> - **ps**——Process Status
> - **top**——Top
> - **netstat**——Network Statistics（网络统计）
> - **pwd**——Print Working Directory  显示当前工作目录
> - **cp**——Copy  复制文件和目录
>- **mv**——Move  移动文件和目录
>- **rm**——Remove  删除文件和目录
>- **mkdir**——Make Directory  创建目录
>- **rmdir**——Remove Directory  删除空目录
>- **touch**——Touch  创建空文件或更新文件时间戳
>- **cat**——Con'catenate  显示文件内容
### 2. 如何查看进程状态（ps、top命令）？

笔记：
- `ps`是查看当前系统中的进程信息，显示命令执行瞬间的进程状态
- `top`提供了动态实时更新的视图，可以持续监测系统中的进程和系统性能
- `ps -a -u -x aux`查看当前终端下所有进程、以用户为主的进程、显示所有进程（不区分终端）、显示所有进程详细信息（包括其他用户）
- `top -c -d -n -p `显示完整命令行不截断、指定刷新秒数、指定n次后退出、指定进程ID信息
### 4. 如何查看文件内容（cat、more、less、tail命令）？ 

笔记：

### 5. 如何查找文件（find、locate命令）？
### 6. 如何查看系统资源占用（CPU、内存、磁盘：vmstat、iostat、df）？
### 7. 如何终止进程（kill、pkill命令）？
### 8. 如何查看网络连接状态（netstat、ss命令）？ 
### 9. Linux文件权限的表示方式（rwx）及修改权限（chmod命令）？
### 10. 软链接和硬链接的区别？如何创建？ 

思路：
[list2pumlMindmap]
- 硬链接和软链接区别
	- 分别是什么
	- 区别
		- 指向
		- 跨文件系统
		- 链接目录
		- 删除原始文件
笔记：
- 硬链接是为文件提供了一个新的名字，指向同一个文件的物理位置，会占用一点资源存储链接名。
- 软链接是某一个文件在另一个位置建立一个同步的链接（快捷方式），不会另外占用资源。
- 硬链接：指向同一个inode；不可以跨文件系统；不可以链接目录；删除原始文件不会影响通过硬链接访问的文件
- 软链接：指向文件路径；可以跨文件系统；可以链接目录；删除原始文件会导致软链接失效（“悬空链接”）
### 11. 环境变量的作用？如何设置和查看环境变量？
### 12. Linux启动流程（BIOS、MBR、GRUB、内核加载、init进程）？

笔记：
1. 开机后，BIOS进行自检，保证硬件能够正常工作，然后根据系统设置的启动顺序查找硬盘，找到MBR主引导记录
2. MBR主引导记录一般在硬盘第一个扇区，包含了引导加载程序的代码和分区表。大小512字节，系统将MBR加载到内存后开始执行，此时，引导加载程序的第一阶段开始
3. GRUB两个阶段：一阶段从MBR加载到内存中；二阶段根据配置文件加载Linux内核和必要的驱动程序，并将控制权交给内核
4. 内核加载后，系统会初始化硬件设备，加载驱动程序，挂载根文件系统。内核会用只读的方式挂载根文件系统，并开始执行初始化过程。此时内核会调用`start_kernel()`函数，启动一系列初始化函数，完成核心环境建立
5. 内核加载成功后，第一个运行的用户空间进程是`/sbin/init`。`init`负责启动系统服务和用户会话
### 13. 进程和线程的区别？

思路：
[list2pumlMindmap]
- 进程和线程的区别
	- 基本单位/容器与执行单元
	- 代码/地址/数据空间
	- 进程与线程关系
	- 崩溃后产生的影响
	- 程序运行出入口/执行序列
笔记：
- 进程是操作系统资源分配的基本单位，线程是任务调度和执行的基本单位；进程是容器；线程是执行单元
- 每个进程有独立的代码和数据空间，切换开销大；同一类线程共享代码和数据空间，有独立的执行栈和pc，切换开销小
- 线程是进程一部分
- 同一进程的线程共享进程的地址空间和资源，进程之间空间和资源是独立的
- 进程崩溃后，不会对其他进程产生影响；但是一个线程崩溃整个进程都崩溃；多进程比多线程要健壮
- 每个独立的进程有程序运行的入口、顺序执行序列和程序出口；但是线程不能独立执行，必须依存在应用程序中，由应用程序提供多个线程执行控制，两者均可并发执行
### 14. 进程的状态（就绪、运行、阻塞、终止）？ 
### 15. 什么是守护进程？如何创建守护进程？
### 16. 线程的状态（新建、就绪、运行、阻塞、死亡）？ 

笔记：
- 线程五态：新建、就绪、运行、阻塞、死亡
- 
### 17. 如何实现进程间通信（管道、信号量、消息队列、共享内存、socket）？
### 18. 线程同步的方法（互斥锁、条件变量、信号量）？
### 19. 什么是死锁？死锁的四个必要条件？ 

笔记：
- 锁资源优先，多个线程互相请求占用锁，导致线程永久阻塞的现象叫死锁
- ==请求与保持==：
- ==不可剥夺==：
- ==互斥==：
- ==环路等待（循环等待）==：
### 20. Linux文件系统的目录结构（/bin、/etc、/home、/proc等）？
### 21. 如何查看内核版本（uname命令）？
### 22. 如何查看系统日志（/var/log⽬录）？
### 23. 如何使用gdb调试程序（断点、查看变量、单步执行）？ 
### 24. Makefile的基本编写规则（目标、依赖、命令）？ 
### 25. 如何编译C++程序（g++命令及常用参数）？ （补充高频考点） 
### 26. [[高频]] select、poll、epoll的区别？为什么epoll效率更高？
### 27. [[高频]] Linux内存管理机制（虚拟内存、页表、缺页中断）？