### 1. Linux常用命令：ls、cd、grep、ps、top、netstat的作用？ 

笔记：
- `ls`：列出该目录下所有子目录和文件
- `cd`：改变工作目录，到指定目录下
- `grep`：查找符合条件的字符串或正则表达式
- `ps`：显示当前进程状态
- `top`：提供系统动态实时视图
- `netstat`：显示网络状态，可显示**TCP/UDP连接、监听端口、路由表、接口统计**等信息

> [!NOTE] 记不住？
> 看全称，
> - **ls**——List
> - **cd**——Change Directory
> - **grep**——Global Regular Expression Print（全局 正则 表达式 打印）
> - **ps**——Process Status
> - **top**——Top
> - **netstat**——Network Statistics（网络统计）
> - **pwd**——Print Working Directory  显示当前工作目录
> - **cp**——Copy  复制文件和目录
>- **mv**——Move  移动文件和目录
>- **rm**——Remove  删除文件和目录
>- **mkdir**——Make Directory  创建目录
>- **rmdir**——Remove Directory  删除空目录
>- **touch**——Touch  创建空文件或更新文件时间戳
>- **cat**——Con'catenate  显示文件内容
### 2. 如何查看进程状态（ps、top命令）？

笔记：
- `ps`是查看当前系统中的进程信息，显示命令执行瞬间的进程状态
- `top`提供了动态实时更新的视图，可以持续监测系统中的进程和系统性能
- `ps -a -u -x aux`查看当前终端下所有进程、以用户为主的进程、显示所有进程（不区分终端）、显示所有进程详细信息（包括其他用户）`ps agx`进程间关系
- `top -c -d -n -p `显示完整命令行不截断、指定刷新秒数、指定n次后退出、指定进程ID信息

### 3. Linux如何创建压缩包（tar、gzip命令）？


### 4. 如何查看文件内容（cat、more、less、tail命令）？ 

笔记：

more 以分页的形式查看
less 可以随意上下翻动
tail 显示文件后几行
head 查看文件前几行

### 5. 如何查找文件（find、locate命令）？


### 6. 如何查看系统资源占用（CPU、内存、磁盘：vmstat、iostat、df）？



### 7. 如何终止进程（kill、pkill命令）？
### 8. 如何查看网络连接状态（netstat、ss命令）？ 
### 9. Linux文件权限的表示方式（rwx）及修改权限（chmod命令）？
### 10. 软链接和硬链接的区别？如何创建？ 

思路：
[list2pumlMindmap]
- 硬链接和软链接区别
	- 分别是什么
	- 区别
		- 指向
		- 跨文件系统
		- 链接目录
		- 删除原始文件
笔记：
- 硬链接是为文件提供了一个新的名字，指向同一个文件的物理位置，会占用一点资源存储链接名。
- 软链接是某一个文件在另一个位置建立一个同步的链接（快捷方式），不会另外占用资源。
- 硬链接：指向同一个inode；不可以跨文件系统；不可以链接目录；删除原始文件不会影响通过硬链接访问的文件
- 软链接：指向文件路径；可以跨文件系统；可以链接目录；删除原始文件会导致软链接失效（“悬空链接”）
### 11. 环境变量的作用？如何设置和查看环境变量？
### 12. Linux启动流程（BIOS、MBR、GRUB、内核加载、init进程）？

笔记：
1. 开机后，BIOS进行自检，保证硬件能够正常工作，然后根据系统设置的启动顺序查找硬盘，找到MBR主引导记录
2. MBR主引导记录一般在硬盘第一个扇区，包含了引导加载程序的代码和分区表。大小512字节，系统将MBR加载到内存后开始执行，此时，引导加载程序的第一阶段开始
3. GRUB两个阶段：一阶段从MBR加载到内存中；二阶段根据配置文件加载Linux内核和必要的驱动程序，并将控制权交给内核
4. 内核加载后，系统会初始化硬件设备，加载驱动程序，挂载根文件系统。内核会用只读的方式挂载根文件系统，并开始执行初始化过程。此时内核会调用`start_kernel()`函数，启动一系列初始化函数，完成核心环境建立
5. 内核加载成功后，第一个运行的用户空间进程是`/sbin/init`。`init`负责启动系统服务和用户会话
### 13. 进程和线程的区别？

思路：
[list2pumlMindmap]
- 进程和线程的区别
	- 基本单位/容器与执行单元
	- 代码/地址/数据空间
	- 进程与线程关系
	- 崩溃后产生的影响
	- 程序运行出入口/执行序列
笔记：
- 进程是操作系统资源分配的基本单位，线程是任务调度和执行的基本单位；进程是容器；线程是执行单元
- 每个进程有独立的代码和数据空间，切换开销大；同一类线程共享代码和数据空间，有独立的执行栈和pc，切换开销小
- 线程是进程一部分
- 同一进程的线程共享进程的地址空间和资源，进程之间空间和资源是独立的
- 进程崩溃后，不会对其他进程产生影响；但是一个线程崩溃整个进程都崩溃；多进程比多线程要健壮
- 每个独立的进程有程序运行的入口、顺序执行序列和程序出口；但是线程不能独立执行，必须依存在应用程序中，由应用程序提供多个线程执行控制，两者均可并发执行
- linux下线程是轻量级进程，lpthread

### 14. 线程是如何实现同步的？ 

思路：

[list2pumlMindmap]
- 常用的线程同步机制
	- 互斥锁mutex
	- 信号量semaphore
	- 条件变量condition variable
	- 读写锁read-write lock
	- 原子操作
	- **屏障barrier**

笔记：
1. 互斥锁mutex：可以保证在==**同一时间只有一个线程可以访问共享资源**==。当一个线程获得了互斥锁后，其他线程需要等待该线程释放锁才能继续访问共享资源。
2. 信号量semaphore：是一种==**计数器，用来控制同时访问某个共享资源的线程数量**==。当计数器大于0时，线程可以访问资源并将计数器减1；当计数器等于0时，线程需要等待其他线程释放资源后才能继续访问。
3. 条件变量condition variable：==**用于线程之间的通信和协调**==。一个线程可以等待某个条件的发生，而另一个线程可以在满足条件时通知等待的线程继续执行。
4. 读写锁read-write lock：==**允许多个线程同时读取共享资源，但只允许一个线程写入共享资源**==。这样可以提高读取操作的并发性，同时保证写入操作的原子性。
5. 原子操作：原子操作是一种不可中断的操作，要么全部执行成功，要么全部不执行。==**原子操作可以保证多个线程同时访问共享资源时的数据一致性**==。
6. **屏障barrier**：用于线程之间的同步，**==屏障允许每个线程在某个点上等待，直到所有线程都到达这个点后再继续执行。==**
### 15. 页面置换算法？ OPT、LRU、LFU、FIFO



### 虚拟内存、物理内存

**零拷贝**：sendfile + DMA 真正意味上的零拷贝

1. **sendfile**： sendfile 是一种系统调用，允许直接将文件数据从内核缓冲区传输到网络套接字，避免了用户态和内核态之间的额外拷贝。它依赖 DMA 技术和文件描述符传递。
2. **mmap**： mmap 将文件映射到用户空间，使用户程序可以直接访问文件内容。通过共享内核缓冲区和用户缓冲区，减少了一次数据拷贝。+write
3. **Direct I/O**： Direct I/O 绕过了内核缓冲区，直接在用户空间和磁盘之间传输数据。这种方式完全避免了内核态的拷贝，但需要硬件支持。
4. **splice**： splice 允许在两个文件描述符之间直接传输数据（如管道和套接字），无需经过用户空间，从而减少了数据拷贝次数。

### 16. free释放的内存会立即还给操作系统吗？——分情况：malloc 和 mar

malloc分配的式虚拟内存

malloc为什么不全部使用memory map分配内存
[malloc 内存分配机制：brk 与 mmap_brk和mmap-CSDN博客](https://blog.csdn.net/JHXXH/article/details/148411529)

### 17. 协程
协程的优势在于高效的上下文切换和对资源的低消耗，非常适合处理大量I/O密集型任务。而线程更适合需要并行计算的场景。

### 18. 管程

### 19. 段错误的五种可能原因：
数组越界、空指针解引用、访问已释放的内存、修改字符串字面量、指针类型错误

### 20. 进程是怎么知道线程崩溃的？（小林）——信号通知整个进程，4个

### 21. 线程专属信号？

### 22. 线程死了，进程一定会死吗

### 23. 信号量和条件变量

### 24. 并行 和 并发

### 25. 孤儿进程 僵尸进程 如何解决

### 26. 解决死锁，中小企业： 无视死锁，直到出现问题再解决。



---
### 14. 进程的状态（就绪、运行、阻塞、终止）？ 

孤儿、僵尸、守护

### 15. 什么是守护进程？如何创建守护进程？

### 16. 线程的状态（新建、就绪、运行、阻塞、死亡）？ 

笔记：
- 线程五态：新建、就绪、运行、阻塞、死亡
- 
### 17. 如何实现进程间通信（管道、信号量、消息队列、共享内存、socket）？
### 18. 线程同步的方法（互斥锁、条件变量、信号量）？
### 19. 什么是死锁？死锁的四个必要条件？ 

笔记：
- 锁资源优先，多个线程互相请求占用锁，导致线程永久阻塞的现象叫死锁
- ==请求与保持==：
- ==不可剥夺==：
- ==互斥==：
- ==环路等待（循环等待）==：
### 20. Linux文件系统的目录结构（/bin、/etc、/home、/proc等）？
### 21. 如何查看内核版本（uname命令）？
### 22. 如何查看系统日志（/var/log目录）？
### 23. 如何使用gdb调试程序（断点、查看变量、单步执行）？ 
### 24. Makefile的基本编写规则（目标、依赖、命令）？ 

笔记：
- 目标
- 依赖
- 命令：必须以tab键开头
### 25. 如何编译C++程序（g++命令及常用参数）？ （补充高频考点） 
### 26. [[高频]] select、poll、epoll的区别？为什么epoll效率更高？

思路：
[list2pumlMindmap]
- select/poll/epoll的区别
	- IO效率
		- 靠什么驱动
		- 时间复杂度
	- 操作方式
		- 遍历方式
	- 底层实现
		- select底层
		- poll底层
		- epoll底层
	- 最大连接数
		- select
		- poll和epoll
	- 对描述符的拷贝
		- select和poll
		- epoll
	- 性能
		- 高并发情况下
		- 连接数少且十分活跃情况下
	- 平台支持
		- select
		- poll
		- epoll
笔记：
- IO效率：select和poll需要无差别轮询所有的描述符集合，时间复杂度为$O(n)$；epoll是事件驱动的，当描述符就绪时，回调函数会把描述符放到就绪队列里面，时间复杂度为$O(1)$
- 操作方式：select和poll采用遍历，而epoll采用了回调
- 底层实现：select底层是数组；poll底层是链表；epoll底层是红黑树
- 最大连接数：select最大连接数为1024或2048；poll和epoll无上限（受限于系统）
- 对描述符的拷贝：select和poll每次调用都会把描述符集合从用户态拷贝到内核态；epoll在调用`epoll_ctl`时会拷贝进内核并保存，之后每次`epoll_wait`时不会再拷贝
- 性能：epoll在高并发的情况下性能远超select和poll；但在连接数少且都十分活跃的情况下，select和poll性能比epoll好（因为要很多函数回调）
- 平台支持：select在Windows、Mac、Linux上都支持；poll是类unix系统都支持；epoll是Linux独有的
[面试必备：对 select，poll，epoll 的详细解析_poll和select和epoll的作用-CSDN博客](https://blog.csdn.net/Geffin/article/details/105364662)
### 27. [[高频]] Linux内存管理机制（虚拟内存、页表、缺页中断）？