### ==README_FIRST==

==蓝字是我自己写的答案，红字是参考答案，我会对自己答案错误的部分进行划线标记和修改，对于我想到的但是参考答案没有的部分我会单独刷色标记（当然，这种情况少之又少），对于我查找补充以及涉及到底层的东西，我会单独插入标注并分享我找到的博客链接。==

**标注list：**
- <font color="#0070c0">蓝字</font>：自己写的
- <font color="#ff0000">红字</font>：参考答案
- 错误：划线
- <font color="#de7802">补充</font>：查到的都没有提到的小点，内容少就橘色，内容多扩展多就插入标注

### 1.简述`const`的作用，`const`与`#define`相比，有何优点。

<font color="#ff0000">const作用：在C语言环境下，用来修饰只读变量，作用在编译时期，所占用的空间有类型；#define是简单的替换，没有类型，作用在预处理时期，不占用空间。const的优点是有类型，可以进行类型检查</font>

<font color="#de7802">还有一个优势，const比#define更好调试，因为#define直接展开了，不方便在调试器里找</font>

<font color="#0070c0">const是一个C语言关键字，他可以使变量的值在程序运行期间保持不变。#define是宏定义，在程序预处理阶段对定义的内容进行替换，const 和 define 相比，</font>~~优点是减轻了程序预处理阶段的代码冗余的压力~~（你tm说什么呢？？？），<font color="#0070c0">在编译阶段会对修饰的变量进行类型检查（define只做替换不检查），</font>~~和define比更节省空间，避免不必要的内存分配~~（你tm说什么呢？？?）。

### 2.头文件中的 `#ifndef`，`#define` ，`#endif` 干什么用？



<font color="#0070c0">`#ifndef #define #endif`是头文件预处理指令，防止头文件的内容被多次包含和编译。</font>

### 3.what's the difference among the data types "char \*p", "const char \*p", "char \* const p"and"const char \*const p" ?

### 4.局部变量能否和全局变量重名.

### 5.请简述typedef int(\*FUN)(int x, int y)及其作用

6.C语言中的main函数返回值有什么用？

7.VC中有哪些方法避免C编程中的头文件重复包含?

8.将程序移植到不同的32位cpu中，经常出现结构字节对齐和大小端的问题，有哪些方法避免？

9.sizeof

struct

{

short a;

long b;

char c;

}d;

sizeof(d)为什么在不同的平台上得到的值不一样？

10.strcpy（）为什么会造成缓冲区溢出？可用哪个函数代替？

  

  

11.strcat的效率问题，有没有更好的解决方案.

12.Heap和Stack有什么区别

  
  

13.请简述大端字节序和小端字节序的概念，设计一个小程序来判断当前操作系统的字节序。

  

14.全局变量和static变量什么区别

  

15.二进制数11101101转换为十八进制数是多少？

  

16.在函数体内声明[1] char *str = "abc";和 [2] char str[] = {'a','b','c'}有什么区别

  

17.strcpy,memcpy,sprintf的区别

  

  

18.define的使用

（1）使用define定义一年有多少毫秒

（2）使用define定义max函数

（3）define中为何经常会使用 do{  }while(0)；来包装多条语句代码

  

  

19.实现一个死循环

20.什么是预编译

  
21.#include<filename.h>和#include"filename.h"有什么区别？

  

22.写出三种交换两个int 变量a 和 b的方法:

  1.______________________________________________________.  

  2.______________________________________________________.  

  3.______________________________________________________.  

  

23.若x,y均是int型变量，则执行以下语句后，写出x,y的值

for(y = 1,x = 1;y<=50;y++)

{

if(x>=10)break;

if(x%2 == 1)

{

x+=5;

continue;

}

x-=3;

}

  

24.写出以下代码段的输出结果

void swap(int m,int n)  

{

int temp;

temp = m;m = n;n = temp;

}

main()

{

int m = 7,n = 5;

swap(m,n);

printf("m = %d,n = %d\n",m,n);

}

输出结果____________________________

  

25.请说明下面例子中const的用途（）

1)const int d = 100;_________________________________________  

2)const char *pStr1 = "ABCD";________________________________________

3)char *const pStr2 = "abcd";________________________________________

4)int func1(int value)const; _________________________________________

  

26.写出以下程序的运行结果_________________________________________

int count(int data)

{

int mask = 1;

int i,count = 0;

for(i = 0;i<sizeof(data)*8;i++)

{

if(data &mask)

{

count++;

}

mask<<=1;

}

return count;

}

data = 0x3; count =_________________________________________ 

data = 0x12345678;count = _________________________________________ 

  

27.下面程序的运行结果是_________________________________________

\#define ADD(a+b) a+b

int main()

{

printf("%d\n",5*ADD(3+4));

}

  

28.在C语言程序中，要给绝对地址0x100赋值，我们可以用：

\*(unsigned int*)0x100 = 1234;

那么，要是想让程序跳转到绝对地址0x100的位置上执行。怎样用一段代码实现_________________________________________

  

29.在C语言中，若要定义一个只允许本源文件中所有函数适用的全局变量，则该变量应该加关键字（）修饰。

  

30.请问执行完下面的程序后x[0]至x[5]的值应为_____________________________

int x[6] = {5,7,4,6,3,8};

int i,k,t;

for(i = 0;i<5;i++)

{

for(k=(i+1);k<6;k++)

{

t = x[i];

x[i] = x[k];

x[k] = t;

}

}

  

31.试着写出结果及原因

unsigned char endian[2] = {1,0};

short x;

x = *(short*)endian;

代码运行后，x的值是多少？ 

  

32.在 int b[][3] = {{1} ,{3 ,2},{4, 5 ,6},{0}};中 sizeof(b)是多少（） 

33.请写出下面代码的输出结果。_________________________________________

void main(void)

{

int a[5] = { 1, 2, 3, 4, 5, };

int \*ptr = (int*)(&a + 1);

printf("%d,%d", \*(a + 1), \*(ptr - 1));

}

  

34.在64位机器下的输出为______________.  

int(\*p[10])(int);         

fprintf(stderr, "%d", sizeof(p));  

35.在little-endian机器，以下代码的输出是______________.

union

{

int i, j;

char a, b;

}x;

int main(void)

{

x.a = 1;

x.b = 2;

x.j = x.a + x.b;

x.i = x.a + x.j;

printf("0x%x\n", x.i);  

return 0;

}

36 .如果x = 2014下面函数的返回值是______________.  

int fun(unsigned int x)

{

int n = 0;

while (x&255)

{

n++;

x = x&(x - 1);

}

return n;

}

37.

\#include <stdio.h>

main()

{

FILE \*fp;

int i, a[4] = { 1, 2, 3, 4 }, b;

fp = fopen("data.dat", "wb");//从这里帮忙解释一下

for (i = 0; i < 4;i++)

{

fwrite(&a[i], sizeof(int), 1, fp);//从这里帮忙解释一下

}

fclose(fp);

fp = fopen("data.dat", "rb");

fseek(fp, -2L * sizeof(int), SEEK_END);//从这里帮忙解释一下 

fread(&b, sizeof(int), 1, fp);//从这里帮忙解释一下

fclose(fp);

printf("b = %d\n", b);  

}

答：________________________________________

38.程序的局部变量存在于_____________中，全局变量存在于________________中，动态申请数据存在于__________________中。

39.若一个视频中图像分辨率为1920 * 1080，每像素采样精度为16 - bit，每秒25帧图像，则每秒图像占内存__________________byte

（所有数据连续存放）。

40.请写出”得到两参数中较小值“的宏：

\#define MIN(a,b)________________________________

41.请写出”判断是否为2的幂次“的宏：

\#define IS_POWER_OF_2(a)___________________________

42.int16_t x = 32767; x++; printf("%d", x);     

输出的结果为_____________________________

  

43.a = 1; a += ++(a++);此时a的值为多少。    

44.count_ones是计算非零整数x的二进制表示下，有多少个bit是1.例如：10 = （1010）2，则返回2，填写空行。

int count_ones(unsigned int x)

{

assert(x > 0);   

int ones = 1;

while (    )ones++;

return ones;

}