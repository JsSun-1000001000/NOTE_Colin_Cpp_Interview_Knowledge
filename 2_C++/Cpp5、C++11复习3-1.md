## 右值引用

**左值：**
- 可以出现在 =赋值 左边和右边的值，有名字，可以取地址
**右值：**
- 只可以出现在 =赋值 右边的值，没有名字，不可以取地址
**纯右值：**
- 运算表达式产生的临时变量、不和对象关联的原始字面量、非引用返回的临时变量、lambda表达式等都是纯右值
**将亡值：**
- 可以理解为即将销毁的值
**左值引用：**
- 对左值进行引用的类型
**右值引用：**
- 对右值进行引用的类型
**移动语义：**
- 转移资源所有权，类似于转让或者资源窃取的意思，对于那块资源，转为自己所拥有，别人不再拥有也不会再使用
**完美转发：**
- 可以写一个接受任意实参的函数模板，并转发到其他函数，目标函数会收到与转发函数完全相同的实参

之前模拟面试挖的坑：
[c++ - Why is it illegal to take the address of the "this" pointer? - Stack Overflow](https://stackoverflow.com/questions/9114930/why-is-it-illegal-to-take-the-address-of-the-this-pointer)
[C++ 之this指针详解_this指针是左值还是右值-CSDN博客](https://blog.csdn.net/qq_45856289/article/details/106477559)
[广义左值-glvalue、左值-lvalue、右值-rvalue、纯右值-prvalue、将亡值-xvalue - 知乎](https://zhuanlan.zhihu.com/p/634413850)
[C++的那些事——左值、纯右值和将亡值 - 知乎](https://zhuanlan.zhihu.com/p/589182834)【this是纯右值少些个纯字】
[C++右值引用，左值(lvalue)、将亡值(xvalue)、纯右值(prvalue)\_c++ xvalue-CSDN博客](https://blog.csdn.net/qq_40586164/article/details/107690601)

**返回值优化：**
- 当函数==需要返回一个对象实例时候==，就会创建一个临时对象并通过拷贝构造函数将目标对象复制到临时对象，这里有拷贝构造函数和析构函数会被多余的调用到，有代价，而通过返回值优化，C++标准允许省略调用这些复制构造函数
```cpp
A a1;
A play(A a);
A a = play(A a1);
```

## 右值引用

左值引用、右值引用、常引用

### 常引用

`const int &i = 10;` 10 是`const int`
### 右值引用

`r-ref rvalue reference`
用&&符号引用右值（也可以移动左值）

```cpp
Data data;
Data& data1 = data;//ok
Data& data1 = Data();//not compile
Data&& data2 = Data();//ok
Data&& data2 = data;//not compile
Data&& data2 = std::move(data);//ok
```